[tool.poetry]
name = "llm-from-scratch"
version = "0.1.0"
description = "An implementation of a modern language model with every step explained from scratch"
authors = ["benedictclarkson1 <benedictclarkson1@gmail.com>"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
numpy = "^1.26.2"
pytest = "^7.4.3"

[tool.poetry.group.dev.dependencies]
black = "^23.11.0"
isort = "^5.13.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
