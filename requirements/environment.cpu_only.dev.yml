name: tricycle

channels:
  - defaults
  - conda-forge
  - pytorch

dependencies:
  # base dependencies
  - python=3.10
  - numpy
  - humanize
  - tqdm
  - mlflow
  - psutil
  # test dependencies
  - scikit-learn
  - pytest
  - hypothesis
  - pytorch # we use this to test correctness, not to cheat
  - pre-commit
  # dev dependencies
  - pyright
  - black
  - isort
  - flake8
  # install tricycle
  - pip
  - pip:
    - -e .
